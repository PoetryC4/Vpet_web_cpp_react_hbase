/*
import React, {useState} from "react";
import {TooltipPlacement} from "antd/es/tooltip";

const petPageProperty = () => {
  const [foodDrawerSwitch, setFoodDrawerSwitch] = useState(false);
  const [drinkDrawerSwitch, setDrinkDrawerSwitch] = useState(false);
  const [medicineDrawerSwitch, setMedicineDrawerSwitch] = useState(false);
  const [presentDrawerSwitch, setPresentDrawerSwitch] = useState(false);
  const [debugDrawerSwitch, setDebugDrawerSwitch] = useState(false);
  const [specialInterDrawerSwitch, setSpecialInterDrawerSwitch] = useState(false);

  const [chatPopoverStyle, setChatPopoverStyle] = useState<React.CSSProperties>({
    display: 'none',
    position: 'absolute'
  });

  const [popoverPlacement, setPopoverPlacement] = useState<TooltipPlacement>('topLeft');

  const [chatResponseDisplay, setChatResponseDisplay] = useState<{ title: any, content: any }>({
    title: 'title',
    content: 'content'
  });


  return {
    foodDrawerSwitch, setFoodDrawerSwitch,
    drinkDrawerSwitch, setDrinkDrawerSwitch,
    medicineDrawerSwitch, setMedicineDrawerSwitch,
    presentDrawerSwitch, setPresentDrawerSwitch,
    debugDrawerSwitch, setDebugDrawerSwitch,
    specialInterDrawerSwitch, setSpecialInterDrawerSwitch,
    chatPopoverStyle, setChatPopoverStyle,
    chatResponseDisplay, setChatResponseDisplay,
    popoverPlacement, setPopoverPlacement,
  }
}

export default petPageProperty


*/
